{
  "name": "webapp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "preview": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "prebuild": "mkdir -p public/styles && cp styles/*.css public/styles/"
  },
  "dependencies": {
    "hono": "^4.10.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "wrangler": "^4.21.1"
  }
}
